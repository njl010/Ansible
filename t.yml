---
  - name: introduction to template
    hosts: dobbi1
    tasks:
      - name: template module
        ansible.builti::n.template:
          src: template.j2
          dest: /home/njl/jinja.conf
          owner:
          group:
          mode:

